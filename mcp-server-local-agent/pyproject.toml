[project]
name = "local-agent-tools"
version = "0.1.0"
description = "Workspace-scoped MCP tools server for local coding agent workflows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.27.2",
]
